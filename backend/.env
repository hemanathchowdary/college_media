# Server
PORT=4000
CORS_ORIGIN=http://localhost:8080

# Database
# LOCAL MongoDB (not working - MongoDB not installed)
# MONGODB_URI=mongodb://127.0.0.1:27017/collegemedia

# MongoDB Atlas (Cloud) - UPDATE THIS WITH YOUR CONNECTION STRING
# Replace <username>, <password>, and <cluster-url> with your actual values
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/collegemedia?retryWrites=true&w=majority

# JWT Secrets
JWT_ACCESS_SECRET=your_secret_access_key_change_in_production
JWT_REFRESH_SECRET=your_secret_refresh_key_change_in_production

# Cookies
REFRESH_COOKIE_NAME=cm_refresh
REFRESH_COOKIE_SECURE=false
REFRESH_COOKIE_SAMESITE=lax
